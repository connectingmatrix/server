# @connectingmatrix/server final runtime contracts

This file is generated by the final process-monitor / project / nodes / workflow-AI / Giga-agents hardening pass.

## Ownership

- package composition
- middleware/API route registration
- GraphQL manifest merge
- MCP generation
- process monitor route exposure
- runtime wiring between packages

## Public API contracts

- `Server.registerPackage(packageModule)`
- `Server.createRuntime(packages,options)`
- `Server.getGraphQLManifest()`
- `Server.getMCPManifest()`
- `Server.getLaunchers()`
- `Server.processMonitoring.list/live/logs.live/abort`

## Routes / launcher

- `GET /server/health`
- `GET /server/process-monitor/list`
- `GET /server/process-monitor/live`
- `GET /server/process-monitor/logs/live`
- `POST /server/process-monitor/abort`
- `GET /server/launcher`

## Notes

- Server binds logger+sockets+process monitor and wires workflows/tree/nodes/projects/chat/agents without owning their business logic.

## Stub launcher

Run the local package launcher with:

```bash
npm run build
node playground.mjs
```
