# Role audit

Composition only: package registration, runtime wiring, GraphQL merge, middleware/routes, MCP/status/launchers.

Stub launcher: `npm run play` or import `createStubLauncher` from `./launcher`.
